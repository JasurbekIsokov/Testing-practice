import { describe, expect, it } from "vitest";

describe("calc", () => {
  it("should return 2 for 1 + 1");
  const actual = 1 + 1;
  const describe = 2;

  expect(actual).toBe(describe); // ===;
});
